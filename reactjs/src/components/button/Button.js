import React from 'react';

const Button = ({ button }) => {
    return (
        <div className="button-container">
            <button button className="shop-btn"></button>
        </div>
    )
}

export default Button;
